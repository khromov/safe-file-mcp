{
  "watch": ["src/**/*.ts"],
  "ext": "ts,json,md",
  "ignore": ["dist/**", "node_modules/**", "*.test.ts", "mount/**"],
  "exec": "npm run build && node dist/index.js",
  "env": {
    "COCO_DEV": "true"
  },
  "signal": "SIGTERM",
  "delay": 500
}
